# Resources Placeholder

Resource payloads are intentionally deferred.

Phase 1 establishes the directory only; later phases will add opt-in resources without forcing opinionated shell startup behavior by default.
